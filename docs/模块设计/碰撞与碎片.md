# 碰撞与碎片

| 项目         | 内容                                                               |
| ------------ | ------------------------------------------------------------------ |
| 模块定位     | 为连续接触、碰撞分类、材料分配、碎片生成和守恒事务提供可信物理底座 |
| 对应代码     | `src/physics/collisions/`                                          |
| 所属里程碑   | [M3：碰撞与碎片](../roadmap.md#m3)                                 |
| 当前状态     | 进行中；Task 1 与 Task 2 已完成，下一步交付连续接触桥              |
| 最近更新时间 | 2026-07-16                                                         |

## 当前状态

M3 Task 1 已交付版本化科学来源、严格输入输出、接触量、EDACM 破坏标度、Genda 合并线、材料剥离、确定性 seed 和 event-total 守恒账本。Task 2 已把正式协议、预览协议、固定太阳系和六类创建预设升级到 v3，碰撞所需资料可在 Worker 往返中无损保存。当前正式推进仍是纯 REBOUND/IAS15 多体引力，连续接触从 Task 3 接入。

M3 将先实现引力主导的分层行星、卫星和当前小行星预设。黑洞使用独立牛顿吞噬规则。恒星碰撞和小于 `1 km` 的强度主导碰撞会安全暂停并说明模型范围。

## 设计

### 职责

- REBOUND C 桥负责 IAS15 推进、连续接触检测、最早接触时刻和碰前快照。
- Rust/WASM 负责 EDACM 分类、材料剥离、残体生成和守恒账本。
- TypeScript Worker 负责运行时校验、候选实例、原子切换、修订号和错误恢复。
- 主线程负责消费版本化事件和状态，不参与物理分类。
- Three.js 负责主要碎块、tracer 和视觉尘埃的显示，画质不会反向影响物理结果。

### 科学模型

主模型采用 Leinhardt-Stewart 2012 的 EDACM，Genda 2012 提供原行星 merge/hit-and-run 临界线，Marcus 2009 只用于论文覆盖范围内的类地行星外层剥离。Benz-Asphaug 1999 的速度相关强度曲线暂不进入首版。

接触输入固定为 SI 质量、半径、位置、速度、自转角动量、转动惯量因子和由外到内的材料层。内部分类保留 merge、hit-and-run、partial accretion、erosion、catastrophic disruption 和 super-catastrophic disruption；界面把它们显示为合并、擦碰、坑蚀、剥离和碎裂。

Task 1 在 `model-sources.ts` 锁定 LS2012 与 Genda2012 的 DOI、arXiv 版本、公式编号和参数。LS2012 的斜碰破坏修正在 `alpha <= 0.5` 时明确标记 `obliquityModelExtrapolated`。Genda 临界线会检查质量比、角度、速度比和总质量；原始两体的成分与自转由候选绑定入口继续核对，任何一项超出校准域都必须标记外推。

### 守恒

碰撞结果必须把全部质量分配到主要残体、tracer 或有物理质量的 dust cohort。GPU visual debris 的质量固定为 `0`，只引用 cohort 做画面采样。线动量、轨道加自转角动量、机械能和耗散项进入独立账本。

| 项目     | 相对误差门槛 |
| -------- | ------------ |
| 质量     | `1e-12`      |
| 线动量   | `1e-10`      |
| 角动量   | `1e-8`       |
| 能量账本 | `1e-6`       |

守恒分为 `event-total` 和 `active-REBOUND`。前者在接触时刻计算主要天体、tracer、dust cohort、自束缚能和 subgrid 机械能的事件闭合；后者只衡量主要天体在碰撞之间的 IAS15 数值漂移。被动碎片省略的反作用冲量、角冲量和功单独累计。

碰撞机械能损失必须进入非负的热、变形、破碎或辐射项。需要负耗散才能闭合的候选会被拒绝。系统诊断分开显示真实碰撞耗散、积分数值漂移、被动模型近似和未展开机械能。

### 连续接触

C 桥在每个 IAS15 受控子步前复制完整 simulation，用真实步首和步尾弦加 `A_rel_bound h^2/8` 曲率上界证明安全区间。无法证明的区间从 checkpoint 回放到中点并递归细分；穿透求最早零点，正切求距离最小值。正式检测不使用 REBOUND 的线性碰撞模式。

距离容差同时考虑接触半径、世界坐标 ULP 和 IAS15 位置误差预算。C 返回最早时刻的全部接触 pair；临时 copy 在成功、失败和回滚路径都必须释放。

### 事务与错误

正式模拟使用 `advanceUntilEvent`。没有接触时推进到目标时间；有接触时停在最早事件。互不共享天体的二体接触从同一碰前快照计算并整批提交；共享天体的三体或更多接触超出首版模型范围，会保留接触态并安全暂停。

Rust 结果通过 schema、容量和守恒检查后，Worker 才创建候选 REBOUND 并原子切换。失败会保留接触时刻的旧实例，销毁候选资源，保持修订号并安全暂停。成功事件通过单个 `collisionBatchResolved` 消息携带同刻事件、账本、新状态和修订号，旧 state 不能覆盖结果。

所有 Worker 成功响应使用 `replyToSequence` 关联原命令，后台推送使用 `null`。运行中的首次不可逆碰撞默认暂停。单步遇到接触时返回包含原目标和实际接触时间的提前暂停结果，因此 controller 不会继续等待原目标。

### 碎片层级

- 每次最多 64 个主要残体，总主要天体不超过 512；它们继续参与完整多体引力。
- tracer 携带代表质量，只受主要天体引力。
- dust cohort 携带聚合质量、位置、速度、材料和 subgrid 机械能，只受主要天体引力。
- tracer 与 dust cohort 共用 10,000 个被动物理资产名额；两类各自也不能超过该上限。
- visual debris 目标上限为 50,000，质量固定为 `0`，只负责画面。
- 固定事件 ID、父体排序和碎片序号生成确定性 seed。
- 容量不足时，质量较小的结果进入 tracer 或 dust cohort，质量不能消失。

## 实现细节

### Task 1 当前实现

| 文件                                | 当前职责                                                    |
| ----------------------------------- | ----------------------------------------------------------- |
| `model-sources.ts`                  | 论文来源、参数、模型范围、容量和守恒硬门槛                  |
| `collision-formulas.ts`             | TS、schema 与后续 Rust 对照共用的 LS2012/Genda2012 纯公式   |
| `schemas.ts`                        | 严格 Zod 结构和候选内部语义复算                             |
| `contact-quantities.ts`             | 接触量、稳定目标体顺序、真实接触距离和正切边界              |
| `disruption-scaling.ts`             | 主破坏曲线、质量比与斜碰修正、适用范围标记                  |
| `classification.ts`                 | 合并、擦碰分离、吸积、侵蚀和两级碎裂分类                    |
| `candidate-validation.ts`           | 把外部候选绑定到原始两体快照和本地选定材料档                |
| `materials.ts`                      | 从 gas 到 iron 的由外向内剥离和绝对材料质量                 |
| `conservation.ts`                   | 质量、材料、线动量、角动量和含耗散机械能的 event-total 总账 |
| `deterministic-seed.ts`             | UTF-8 字节序、长度前缀和 FNV-1a 64 的跨语言确定性 seed      |
| `fixtures/collision-golden-v1.json` | TS/Rust 共用黄金值，包含 `alpha > 0.5` 的完整斜碰公式链     |

候选数据先做内部自洽复算：接触派生量、八个破坏标度字段、最大残体、离散分类、Genda 临界值和外推标记都必须与本地公式一致。离散边界只使用本地重算值，防止候选利用浮点容差跨过 `1.0`、`1.8` 或 Genda 等号。`parseCollisionResolutionCandidateForInput` 随后用原始两体快照重算接触量，并锁定本地材料档，阻止一套自洽但属于其他天体或其他材料模型的数据进入事务。

经典 event-total 账本只接受引力固体和引力流体参与碰撞。完整事件状态可以保留未参与的黑洞；黑洞成为参与体时进入 Task 4 的独立吞噬账本。吞噬后的材料来源记录也在 Task 4 处理。

### Task 2 当前实现

- `BodyState` 固定携带材料层、自转角动量、转动惯量因子和碰撞模型；零半径点质量继续兼容，碰撞调度会在后续任务判断支持范围。
- `PhysicsState` 分开保存主要天体、tracer、dust cohort、累计碰撞摘要、省略相互作用、反作用近似和双层诊断。完整逐事件账本只放在批次增量中，避免每帧状态持续变大。
- `collisionBatchResolved` 原子携带原目标、接触时间、事件、账本增量、修订号前后值和完整新状态。观测台收到批次后立即暂停并清除排队中的旧修订帧。
- controller 按 `replyToSequence` 精确关联命令。`step` 接受正常目标帧或提前碰撞批次；后台消息使用 `null`，无法完成其他请求。
- REBOUND 继续只负责质量、半径、位置和速度等动力学字段；适配层保存并深拷贝其他物理资料，在每次快照中合并。
- 固定太阳系 10 体和六类创建预设已经补齐 v3 资料。太阳系材料比例属于 STARY 四材料桶近似，来源和边界记录在场景模块文档。

计划目录边界：

| 位置                                       | 职责                                         |
| ------------------------------------------ | -------------------------------------------- |
| `src/physics/collisions/`                  | 来源锁、纯函数参考、schema、账本与 Rust 适配 |
| `crates/stary-collision/`                  | 固定 Rust 碰撞内核和 golden fixtures         |
| `spikes/rebound-wasm/src/rebound_bridge.c` | 连续接触与事件读取 C 导出                    |
| `src/physics/runtime/`                     | 事件推进、批次事务和被动碎片推进             |
| `src/physics/protocol/`                    | 协议 v3、事件、材料、自转和账本              |
| `src/features/observatory/`                | 碰撞状态、事件面板和碎片画面                 |

完整公式、七个任务和测试矩阵见 `docs/2026-07-15-stary-m3-collisions-debris.md`。

## 验证

- Task 1 当前有 6 个测试文件、41 项测试，覆盖论文黄金样例、分类边界、候选篡改、材料、守恒和 seed。
- 完整斜碰黄金链同时覆盖 `alpha=0.443` 的算术锁和 `alpha=0.885` 的 LS2012 适用区样例。
- 数值边界覆盖 `1e200 kg`、可表示的 `1e308 kg` 总质量、中间乘法防溢出、不可表示总质量拒绝、UTF-8 非 BMP 顺序和孤立 UTF-16 半个字符拒绝。
- Task 2 的协议测试覆盖 v1/v2 拒绝、BodyState 必填资料、PhysicsState、碰撞事件与账本一一对应、目标时间和修订号边界。
- Worker 与 controller 测试覆盖精确回执、后台帧隔离、并发同类响应不串单、原子替换和 REBOUND 新资料无损往返。
- 观测台状态测试覆盖碰撞批次即时应用、强制暂停、旧修订拒绝和 RAF 缓冲清理；预览协议 70 项定向测试通过。
- 完整门禁通过 57 个 Vitest 文件共 390 项测试、生产构建与产物校验、28 条 Playwright；四个性能场景的 Worker state 中位频率均约为 `59 次/秒`。
- 连续接触、Rust 对照、正式碰撞事务、事件面板和碎片画面证据分别在 Task 3 到 Task 6 补齐。

## 已知边界

- EDACM 是分析标度模型，不是逐单元流体仿真。
- 气态巨行星使用无强度流体近似。
- 恒星、小于 `1 km` 的强度区和完整相对论黑洞并合不在首版支持范围。
- passive tracer 和 dust cohort 不产生主动引力；省略反作用会明确进入模型近似诊断。visual debris 没有物理质量。
- Task 2 会校验被动资产诊断的总质量、线动量和轨道角动量。`totalMechanicalEnergyJoules` 还依赖主要天体与被动资产的势能；Task 4 首次生成被动资产时必须建立统一重算函数，并在天体替换后刷新该值。
- M4 才实现碰撞事件的快照、撤销和确定性历史回放。

## 改动历史

### 2026-07-16：完成 M3 Task 2 协议与场景资料

- 正式与预览协议升级到 v3，增加精确回执、完整状态、碰撞批次和双层诊断。
- 固定太阳系与创建预设补齐材料、自转、惯量和碰撞模型。
- 创建、编辑、删除、轨道预览、REBOUND 和观测台状态链完成迁移与无损测试。

### 2026-07-16：完成 M3 Task 1 纯函数底座

- 交付来源锁、接触量、破坏标度、分类、材料剥离、event-total 守恒账本和确定性 seed。
- 增加候选全链语义复算、原始输入与材料档绑定，锁住分类和浮点边界篡改。
- 增加 LS2012 斜碰外推标记、Genda 数值范围标记和高交互斜碰黄金样例。
- 明确经典账本与 Task 4 黑洞吞噬账本的边界。

### 2026-07-15：锁定 M3 实施边界

- 选择 EDACM、Genda 和 Marcus 的来源边界，记录小天体、恒星和黑洞的处理方式。
- 确定 REBOUND 检测、Rust 分类、TypeScript 原子事务和 Three.js 显示的职责。
- 确定协议 v3 的材料层、自转、碰撞事件和守恒账本。
- 拆分七个独立任务并固定测试、容量和性能门槛。
