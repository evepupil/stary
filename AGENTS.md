# STARY AI 开发规范

## 沟通

- 默认使用中文，保留必要的代码、命令和错误原文。
- 说明实现、设计和结果时使用短句和常用词；专业术语首次出现时补一句通俗解释。
- 避免使用对立式否定转折句。

## 工作范围

- 这些规则适用于本仓库中的 AI 辅助开发、测试、评审和提交。
- 修改应围绕当前任务，禁止顺手进行无关的大范围重构。
- 历史代码不符合新规则时，只改当前触及的范围，除非任务明确要求清理。
- 默认在 `main` 分支直接开发与提交，用户另有要求时除外。

## 开发流程

1. 新需求先给出实现方案。用户没有异议时继续实现，无需逐步等待批准。
2. 出现会改变产品行为、公共数据结构、兼容性或主要技术路线的取舍时，停止并请用户决策。
3. 实现结束后运行仓库中真实存在的格式化、静态分析、类型检查、测试和构建门禁。
4. 门禁全部通过后检查暂存差异，再使用 Conventional Commits 提交。
5. 多个独立需求按合理边界拆成多个提交，提交信息从实际差异生成。

## 当前工具链

- 语言与运行时：Node.js `22.14.0`；TypeScript `6.0.3`，应用与构建配置均开启 `strict`。
- 包管理与构建工具：pnpm `10.21.0`、Vite `8.1.4`、React `19.2.7`、Three.js `0.185.1`。
- 测试与代码质量：Vitest `4.1.10`、Playwright `1.61.1`、ESLint `10.7.0`、Prettier `3.9.5`。
- 运行时结构校验：Zod `4.4.3`；跨线程未知输入必须先解析，再进入业务逻辑。
- 干净安装命令为 `pnpm install --frozen-lockfile`。
- 格式检查命令为 `pnpm format:check`，修复当前工程文件格式使用 `pnpm format`。
- 静态检查命令为 `pnpm lint`，严格类型检查命令为 `pnpm typecheck`。
- 单元与物理集成测试命令为 `pnpm test`；它会直接运行固定 REBOUND WASM 的圆轨道、椭圆轨道和 1000 周期守恒测试。生产构建命令为 `pnpm build`。
- 前端完整门禁为 `pnpm check`，依次运行格式、lint、类型、单测、生产构建和 Playwright 浏览器验收。首次在新环境运行前使用 `pnpm test:e2e:install` 安装 Chrome。
- 浏览器验收也可单独运行 `pnpm test:e2e`。它覆盖非空画布像素、暂停、单步、倍率、恢复运行、手机抽屉和 WebGL2 真实回退。
- 当前观测台性能采样命令为 `pnpm test:performance`。它构建生产包，通过短生命周期 Vite preview 采样 2 秒 RAF 和 Worker state 序号推进频率，完成后自动退出；历史 M0 双天体基线以验收文档中的固定提交和数值为准。
- `pnpm build` 会检查生产包不含 source map，验证正式 physics Worker、观测场景与 WebGPU/WebGL 渲染模块均已按需产出，并按 `artifact-lock.json` 校验 REBOUND 胶水源文件、原始 WASM 和发布 WASM。
- 正式观测台通过 `useUniverseSimulation` 驱动 JPL J2000 太阳系 10 体 Worker 生命周期；画面优先使用 WebGPU，初始化失败时回退 WebGL2。天体 mesh 使用真实半径，屏幕空间定位环只负责可见性和选择。
- REBOUND/WASM 隔离原型使用固定 Docker 镜像构建，命令为 `spikes/rebound-wasm/scripts/build.ps1`。
- 原型测试命令为 `node --test spikes/rebound-wasm/tests/*.test.mjs`，数值报告命令为 `node spikes/rebound-wasm/scripts/run-acceptance.mjs`。
- 原型完整门禁为 `powershell -NoProfile -ExecutionPolicy Bypass -File spikes/rebound-wasm/scripts/verify.ps1`，包含固定构建、测试、数值验收和产物哈希校验。
- 文档变更至少运行 `git diff --check` 并检查暂存差异。

## 代码质量

- 语言或编译器提供严格模式时必须开启。TypeScript 使用 `strict`；Rust 代码保持 Clippy 无新增警告。
- 按职责拆分模块，避免把物理、渲染、交互、存储和界面逻辑堆进同一文件。
- 共享数据必须有明确类型和单位；跨线程消息必须做运行时校验。
- 物理层使用真实单位，显示换算留在界面或展示适配层。
- 避免引入与目标无关的抽象；新增依赖前确认用途、维护状态和浏览器兼容性。

## 文档与路线图

- 项目进度维护在 `docs/roadmap.md`。完成里程碑、改变范围或确认重大决策时同步更新。
- 总体产品规格和实现计划直接维护在 `docs/`。
- 因项目已有路线图，每个合理的大模块都要在 `docs/模块设计/<模块>.md` 维护设计、改动历史和实现细节。
- 模块实现或修复与对应归档文档在同一提交中更新，保证文档描述当前实现。

## 测试

- 物理积分、碰撞分类、守恒计算、历史回放、存档迁移、状态计算和纯函数必须有单元测试。
- 跨线程协议、WebAssembly 边界和导入导出使用高信号集成测试。
- 关键用户流程使用浏览器端到端测试，断言真实行为和画布输出。
- 主题、配色、布局和组件外观禁止写单元测试，使用桌面与手机截图检查。
- 修复缺陷时应先写能复现问题的回归测试；无法添加时说明原因和手动验证方法。
- 不为满足形式而模拟简单依赖，优先测试真实行为。

## 格式化、静态检查与构建

- 使用项目已配置的命令，禁止在历史格式不一致时运行造成全仓无关改动的格式化。
- 行为、公共类型或协议发生变化时，完成前必须运行类型检查、相关测试和构建。
- 某项门禁未建立或无法运行时，明确报告缺口，不得声称全部通过。

## 调试与本地服务

- 禁止自行启动开发服务器，用户明确要求时才可启动。
- 读取文件遇到错误或乱码时，使用全局命令 `jiemi.exe <文件路径>` 解密后再继续。
- 排障先获取真实错误、输入和运行状态，再提出修复方案。

## CodeGraph

- CodeGraph 已初始化时，符号定义、调用关系、影响范围和模块结构优先使用 `codegraph_*` 工具查询。
- 字符串、注释、日志和已知文件中的字面内容使用 `rg`。
- `.codegraph/` 不存在时先询问用户是否运行 `codegraph init -i`，未经确认不初始化。
- 文件写入后给索引留出同步时间，避免立即查询旧数据。

## AI 提交前检查

- AI 协助提交时必须查看 `git diff --cached`。
- 检查缺失测试、边界值、并发与生命周期、数据一致性、意外大范围改动和敏感信息。
- 严重或主要问题在提交前修复；决定保留时明确记录原因。
- 使用 Conventional Commits：`<type>(<scope>): <summary>`。
- 常用类型包括 `feat`、`fix`、`refactor`、`test`、`docs`、`chore`、`build`、`ci`、`perf` 和 `revert`。

## Review 记录

- Review 记录放在 `docs/review/`，目录缺失时在首次 Review 创建。
- `docs/review/_剩余问题.md` 常驻维护，抬头记录上次 Review 结束提交，正文只保留未解决问题和已确认不改的取舍。
- 每次 Review 新建 `YYYY-MM-DD-<简述>.md`，写明起止提交和本次全部发现。
- 新问题同时写入单次记录与总览；已解决问题从总览删除，并在单次记录中标记已解决。
- 业务决定不修改的问题在两处都记录原因。
- 首次 Review 没有历史起点时向用户确认起始提交。

## 完成条件

- 运行能够证明本次结论的最新验证命令，并阅读完整结果。
- 检查 `docs/roadmap.md` 和相关模块归档是否需要更新。
- 工作区只包含预期改动，提交与状态信息来自当前仓库。
- 报告已运行的门禁、未运行项目及原因，禁止用推测代替验证。
